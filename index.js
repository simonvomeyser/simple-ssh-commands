
try {
    
    var config = require('../../.ssh-commands')

} catch (error) {

    console.warn("Simple SSH Commands: You are missing an '.ssh-commands' file in your root directory.")
    
} 

return;
